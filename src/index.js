const app = require("./app")
// TODO const transaction = require("./services/transactions")

// CONNECT DATABASE
// TODO transaction("Uploader")

// LISTEN APP
app.listen(3000, () => console.log("Uploader listening on port 3000!"))
